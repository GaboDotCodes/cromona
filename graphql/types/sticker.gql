type Sticker {
  album: Album
  number: Int
  title: String
  image: String
  haveRepeated: [User] #ToCoding
}
input StickerInput {
  album: ID
  number: Int
  title: String
  image: String
}
type Mutation {
  addSticker(sticker: StickerInput): Sticker
}
